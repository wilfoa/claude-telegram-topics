{
  "mcpServers": {
    "telegram-topics": {
      "command": "bash",
      "args": [
        "-c",
        "(cd \"${CLAUDE_PLUGIN_ROOT}\" && bun install --no-summary) >&2 && exec bun \"${CLAUDE_PLUGIN_ROOT}/shim.ts\""
      ]
    }
  }
}
